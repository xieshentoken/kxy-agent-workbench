# V10 settings usability

User authorizes implementation by existing Luna max session; parent plans and accepts. README by Luna, no acceptance report. Follow AGENTS.md, back up first, direct changes to existing modules, no new abstract layers. Reuse current settings/discovery/output mechanisms.

1. Agent check and refresh: clear per-agent Check and refresh action and Check and refresh all. Reuse discover_agent for binary/version/models/effort. Add separate sanitized login status with checked_at, evidence/source and useful reason. Use verified read-only CLI status commands where available (inspect local help); otherwise distinguish credential/config present from verified login, unknown and unsupported. Never equate model catalog with authenticated access or chargeable inference. Never launch login/logout/browser auth, refresh/rewrite credentials or run inference. Do not expose tokens/account payloads or store secrets. Timeouts and bounded concurrency, individual failures do not abort all agents, no duplicate concurrent scans or late response overwriting another selected agent. Optional persistent check-on-settings-open toggle; default off to avoid unsolicited startup work. Preserve configured executable paths, saved aliases and API bindings. Native catalog refresh should not erase valid saved analyzer choices on transient discovery failure. UI refresh agents and model dropdowns together, with busy state and last result.

2. Default analyzer: global setting supports a saved analyzer model binding (agent + alias + exact model + effort) or plain installed agent CLI if user has not saved a binding. Backend validates selected references; only refs/plain CLI persist, never key. Apply to newly created analyzers, including freshly created loop executor/reviewer. Do not overwrite imported workflows, existing nodes, template explicit bindings or applied custom presets. Show current effective default and clear/fallback action; deleted/unavailable bindings show actionable missing state rather than silently switching to a different paid model. Reuse existing binding application helper for exact model/effort. Refresh persists preference.

3. Output settings: User explicitly confirmed output-content configuration. Scope is a settings Output tab explaining actual generated files and defaults for new output containers: result.txt / result.md / result.json (including content/full JSON option), actual generated files in files/, provenance.json / output-manifest.json and receipts used for traceability/resume. Derive explanations from current actual layout; do not claim unused data/outputs directory is current destination. Display managed output location read-only plus explicitly authorized destination choices if supported. Configure default export formats, allowed artifact extensions and JSON mode using existing validators; apply only to new containers, preserve old explicit node choices and snapshots. Recovery/security manifests mandatory with explanation, no disabling internals or deletion/retention sweep. Never grant a path merely through settings or workflow import.

Acceptance: keep small, approximately 3-4 focused synthetic tests for scan partial failure/redaction, default reference persistence/application, real generated output file selection and boundary preservation. TypeScript production build to dist-v10. Parent does one focused UI journey and verifies actual publication/data preservation. No full historical suite, no report needed, no paid inference. Leave formal8710 and frontend/dist unchanged until parent accepts. Keep README clear on usage and verified/unknown login distinction.
