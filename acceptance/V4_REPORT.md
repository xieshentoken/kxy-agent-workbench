# kxy V4 独立验收记录

日期：2026-09-07。规划见 [V4_PLAN.md](../docs/V4_PLAN.md)，使用方式见 [README.md](../README.md)。

由既有 GPT-5.6 Luna / max session `01a07774-30b2-7d60-b059-cbc66dc4617a` 完成代码，父任务负责方案、差异审查、反例检查、浏览器验收及本机发布。未创建额外实现任务。

## 已交付

- 画布增加明确的移动/框选工具；框选多张卡片后可建立黑盒，并可撤回。复用既有单输入、单输出及最多四层嵌套边界，不能无损封装时显示说明。
- 分析器可选择 Markdown、纯文本、JSON；支持 JSON Schema 编辑、导入、校验和导出。历史 `expect_json=true` 映射为 JSON。格式决定真实输出文件后缀；JSON 需为单个对象，并按可选 Schema 校验。
- 容器可选择 `.md`、`.txt`、`.json`，JSON 可输出完整记录或内容；保留 provenance。各选项提供悬停说明。旧流程默认文件保持兼容。
- 运行面板显示实际正文，允许编辑、导入、下载正文与 JSON 副本。无效 JSON 不允许作为有效 JSON 下载；副本修改不改写已完成运行及原始产物。
- 拖尾及选中卡片扩散只高亮实际 XYFlow SVG 网格点：位置、间距、半径跟随画布变换，卡片内部不绘制高亮；无活动时清空并停止帧循环，遵守动效开关和系统减少动态效果设置。
- Agent 配置解释原生、手动和 API 模式。增加 API key/已保存凭据的连接测试，结果显示实际 Endpoint。已保存凭据使用绑定 Endpoint。请求仅获取模型目录，不进行推理；不跟随重定向，不回显密钥或上游错误正文。

## 独立检查结果：PASS

后端共 **45 个不同测试**，均使用隔离数据与合成输入：

| 脚本 | 通过数 | 主要范围 |
|---|---:|---|
| `v4_supervisor_checks.py` | 14 | 输出格式与真实文件、JSON Schema/引用限制、容器内容类型、API 鉴权及错误分类、敏感信息不回显、非阻塞连接测试 |
| `supervisor_checks.py` | 7 | 既有核心执行与导出边界回归 |
| `v3_supervisor_checks.py` | 11 | V3 设置、技能映射与配置边界回归 |
| `v2_runtime_checks.py` | 13 | V2 运行时与节点回归 |

日志保存在 [v4-evidence](./v4-evidence/)。实现 session 的重复执行不另计测试数。

前端 TypeScript 检查及 Vite 生产构建通过。父任务在隔离服务 `8720`、本地模拟模型目录 `8740` 上执行 Playwright/Edge 验收，共 **22 组 PASS**，无浏览器运行错误。明细见 [results.json](./v4-evidence/independent-browser/results.json)。覆盖：

- 框选 → 黑盒 → 撤回；工具栏与快速开始布局。
- 初始、缩放、平移后的鼠标/选中卡片高亮，与真实 SVG 网格点几何对齐；卡片内部排除、扩散自然结束、空闲清空。
- 历史 JSON 模式、Schema 导入/应用/导出，保留 `keywords`、`token_count` 等合法字段；切换纯文本不残留活动 Schema。
- 实际 LFX 运行正文显示、无效 JSON 下载拦截、输出副本编辑/导入/下载且原产物不变。
- API 表单使用合成 key 调用本地模型目录，无 Keychain 持久化；代码字号继承、减少动态效果设置。
- 1280×720 画布可访问、390px 页面无文档级横向溢出。桌面和窄屏截图经父任务目视检查；这不等于移动设备全功能验收。

最终 22 组中，前 16 组与后 6 组分段完成；中间调整了测试的标签定位以匹配带提示文字的表单。分段间应用代码/构建未变，最终结果合并记录。早期失败截图用于保留诊断历史，不代表最终状态。

## 本机发布与数据保留：PASS

- 实现前备份：`.backups/pre-v4-20260907-153846/`。
- 发布前静态资源备份：`.backups/pre-v4-publish-20260907-163659/dist/`。
- 验收产物 `frontend/dist-v4` 已发布到 `frontend/dist`；先复制资源、最后替换 HTML，保留旧哈希资源供已打开页面使用。
- 重启前无运行中/等待确认任务。正式服务已加载当前后端，地址为 `http://127.0.0.1:8710/`，发布时 PID 为 `11348`。
- 正式 HTTP 健康检查、V4 路由及静态资源均通过；HTTP 返回资源与验收产物逐字节一致：`index-Bokv2bTC.js`、`index-DMWTZgsZ.css`。
- 文件、技能、已保存流程、运行历史及设置 API 的 SHA-256 与本轮修改前一致。见 [formal-verification.json](./v4-evidence/formal-verification.json)。未向正式服务写入测试资料、测试 key 或运行。
- 历史 `kxy-v3-macos-arm64-20260907.zip` 保持原哈希 `283291433397fc3a9b39b6c525aa28935699dab50c9f304c1cde0ba1a1ba7234`，本轮未重新制作分发包。

## 验证边界

本轮没有调用真实第三方模型推理，没有写入新的真实 Keychain 凭据，也没有进行跨设备安装、Windows/Linux、并发负载或真实私有资料验收。连接成功仅表明 `/models` 可连接/鉴权，不能证明指定模型的推理权限或 CLI 对任意 Endpoint 的兼容性。API 模式的实际分析仍经所选 CLI 执行。

JSON Schema 只允许本地引用，不从外网解析引用。输出编辑作用于下载副本；撤回仅作用于画布编辑，不能撤销已执行模型调用或文件写入。
